pekko {
  log-dead-letters = 10

  # Uncomment to debug remote messages. Make sure you adjust logback.xml too.
  #remote {
  #  log-sent-messages = true
  #  log-received-messages = true
  #  log-remote-lifecycle-events = info
  #}
}